# Reference Navigation

Canonical reference artifact locations for this repository (directories scaffolded; files populated in subsequent tasks).

## Structure

- `raw-handoff/2026-04-19/` - immutable Claude Design export artifacts
- `reference/` - canonical external/reference artifacts (e.g., `karpathy-llm-wiki.md`, `phase-0-governance-audit-2026-04-22.md`, `branch-protection-main-2026-04-22.json`)

## Rules

1. Files under `raw-handoff/` are immutable.
2. Canonical operational docs live under `.opencode/context/project-wiki/`.
3. `docs/` and `IMPLEMENTATION_PLAN.md` are temporary legacy backup only.
